# SkillHone Python dependencies
# -----------------------------------------------------------------------------
# Install:
#   pip install -r requirements.txt
#
# Recommended inside a virtualenv:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# -----------------------------------------------------------------------------

# ── Core open-source dependencies ────────────────────────────────────────────

# YAML parsing for skill metadata and static checks
PyYAML==6.0.3

# Runtime maintenance uses the Python standard library plus Git. DeepSeek
# Harness runs as a separate Node.js process, so no Agent SDK, Anthropic client,
# LiteLLM proxy, HTTP client, or GitPython package is required here.

# ── Development / test dependencies (optional) ──────────────────────────────

# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# pytest-httpserver>=1.0.10

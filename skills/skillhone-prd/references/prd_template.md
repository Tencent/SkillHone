# PRD — <skill-name>

> Generated by skillhone-prd on <DATE>. **Eval-visible** copy. This file
> lives in `<skill-name>-eval` (orchestrator-only). It must **never** be
> copied into the skill repo. The improver-visible companion file
> `PRD.improver_only.md` is auto-derived from this one with section 4
> and any auto-validation rules redacted — do not hand-edit it. The
> interview transcript is written separately to `PRD.choice.md`. When
> publishing to Forgejo wiki, this full copy belongs only in the
> `<skill-name>-eval` wiki; the skill repo wiki receives only the redacted
> improver-visible PRD.

## 1. Environment

- **Tools the skill may call**: <e.g. WebSearch, Bash(read-only), Read, MCP:x>
- **Tools explicitly forbidden**: <e.g. any network egress, subprocess.Popen>
- **Runtime / language assumptions**: <e.g. Python 3.12, no external pip installs>
- **External services / APIs / MCP servers**: <name + purpose, or "none">
- **Filesystem scope**: <paths the skill is allowed to read / write>

## 2. Goal

- **Primary optimization goal (one sentence)**: <...>
- **Non-goals** (things this skill explicitly should NOT try to do): <...>
- **Primary user / caller** (who invokes this skill): <...>
- **When the skill should NOT be invoked**: <...>

## 3. Output format

- **Format type**: <JSON schema | Markdown sections | CSV | code | free text | ...>
- **Exact schema or template**:
  ```
  <paste the literal template or JSON schema here>
  ```
- **Worked example of a valid output**:
  ```
  <a concrete, minimal example>
  ```
- **Automatic validation rule**: <jsonschema file | required Markdown headings list | regex | unit-test command>

> The `Automatic validation rule` field is stripped from
> `PRD.improver_only.md` to keep the validator out of the improver's
> sight.

## 4. Evaluation

> This entire section is dropped from `PRD.improver_only.md`.

- **Primary metric**: <name + definition + how it's computed>
- **Pass threshold on the primary metric**: <e.g. score ≥ 0.8>
- **Secondary metrics (tie-breakers)**: <ordered list, each with definition>
- **Grading function (outline)**: <rule-based | LLM-as-judge + rubric | hybrid>
- **What counts as a pass**: <concrete criteria>
- **What counts as a catastrophic fail** (hard-zero): <e.g. schema violation, leaking PII>
- **Eval dataset shape**: <probe.jsonl / pr_val.jsonl layout — fields, size, source>
